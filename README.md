# di-b18-infra
#Azure Git Learning
#Git status use to display the current status of working directory. it tall me which directory are modify stage, untrack and my local branch is syn to the remote respositry.
git add README.md  modify the file and move into the stagin table .
git commit -m "Updated file Readme"
git status check the stas 
git  push     update commit filr intot the remote repositry
git staus 
git merge 

